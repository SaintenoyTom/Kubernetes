apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-app
  template:
    metadata:
      labels:
        app: node-app
    spec:
      containers:
        - name: node-app
          image: kubernetes-node:1.0
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: config-volume
              mountPath: /app/config/config.json
              subPath: config.json
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: node-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: node-secret
                  key: DB_PASSWORD
      volumes:
        - name: config-volume
          configMap:
            name: node-config